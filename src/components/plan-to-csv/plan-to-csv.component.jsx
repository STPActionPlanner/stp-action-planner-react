import { removeFragmentSpreadFromDocument } from '@apollo/client/utilities'
import React from 'react'

const PlanToCsv = ({plan}) => {

  const createCSV = () => {
    // reformat = goal | activity | 
  }

  return (
    <button>
      Convert to csv.
    </button>
  )
}

export default PlanToCsv
